# Copy this file to `.env` and fill in your key:  cp .env.example .env

# --- Qwen (Alibaba Cloud DashScope, OpenAI-compatible) ---
DASHSCOPE_API_KEY=sk-91f25440c45040ebacd0b898051ed3ae
# International endpoint (default). For mainland China, use:
#   https://dashscope.aliyuncs.com/compatible-mode/v1
DASHSCOPE_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
# qwen-plus (balanced) | qwen-max (best reasoning) | qwen-turbo (cheapest)
QWEN_MODEL=qwen-flash

# --- Data ---
# Path to the deduped PSX enforcement DB. Defaults to ./data/psx.db if unset.
DB_PATH=data/psx.db

# --- Postgres checkpointer (used later for cross-session memory; optional now) ---
# DB_URL=postgresql://user:pass@host:5432/dbname
# DB_URL_LOCAL=postgresql://postgres:postgres@localhost:5432/postgres
